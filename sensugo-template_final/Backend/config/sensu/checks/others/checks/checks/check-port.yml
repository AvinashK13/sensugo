---
type: CheckConfig
api_version: core/v2
metadata:
  name: check-port
  namespace: default
spec:
  command: check-ports.rb
  handlers: [email,slack]
  interval: 10
  publish: true
  runtime_assets:
  - network-check
  - sensu-ruby-runtime
  subscriptions:
  - system

